using System;

namespace ЯЮниор
{
    class Program
    {
        static void Main(string[] args)
        {
            double timeInSecond = 90.5;
            uint distanceInMeters = 181;
            double speedInMetersPerSecond = distanceInMeters / timeInSecond;
            double maxSpeed = 150;
            bool violation = false;
            string driversName = "Alesha";
            char firstLetterOfName = 'A';
            ulong carMileageInMeters = 14902231;
            ushort carNomber = 123;
            string carBrand = "Toyota";
            Console.WriteLine(speedInMetersPerSecond);
        }
    }
}
